<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.urbanutils">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ProviderChatActivity"></activity>
        <activity android:name=".ShowRequestedUserActivity" />
        <activity android:name=".CustomerChatActivity" />
        <activity android:name=".ProviderLoginActivity" />
        <activity android:name=".MapActivity" />
        <activity android:name=".customerService" />
        <activity android:name=".serviceList" />
        <activity android:name=".approveProvider" />
        <activity android:name=".providerList" />
        <activity android:name=".RegisterProviderActivity" />
        <activity android:name=".ProfileEditActivity" />
        <activity android:name=".MyProfileActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".VerifyPhoneActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>